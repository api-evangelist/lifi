generated: '2026-07-19'
method: generated
source: >-
  Generated by the API Evangelist enrichment pipeline from
  openapi/lifi-openapi-original.yml and openapi/lifi-earn-openapi-original.yml,
  grounded in conventions/lifi-conventions.yml, errors/lifi-error-codes.yml and
  rate-limits/lifi-rate-limits.yml. LI.FI publishes no first-party Agent Skill
  files or AGENTS.md, so none could be captured verbatim; it does publish a
  dedicated agent documentation surface at https://docs.li.fi/agents/overview
  which these skills follow.
grounding_note: >-
  The LI.FI API OpenAPI defines no operationIds, so operations in these skills
  are referenced by verified method + path. The Earn API does define
  operationIds and they are used verbatim.
skills:
- file: lifi-cross-chain-transfer.md
  name: Execute a cross-chain token transfer with LI.FI
  api: openapi/lifi-openapi-original.yml
  operations:
  - GET /v1/chains
  - GET /v1/token
  - GET /v1/connections
  - GET /v1/quote
  - GET /v1/status
- file: lifi-discover-and-deposit-yield.md
  name: Discover a yield vault and deposit into it with LI.FI Earn and Composer
  api: openapi/lifi-earn-openapi-original.yml
  secondary_api: openapi/lifi-openapi-original.yml
  operations:
  - ChainsController_getChains_v1
  - ProtocolsController_getProtocols_v1
  - VaultsController_listVaults_v1
  - VaultsController_getVault_v1
  - PortfolioController_getPositions_v1
  - GET /v1/quote
  - GET /v1/status
- file: lifi-compare-routes-and-execute.md
  name: Compare multiple LI.FI routes and execute a chosen one step by step
  api: openapi/lifi-openapi-original.yml
  operations:
  - GET /v1/tools
  - POST /v1/advanced/routes
  - POST /v1/advanced/stepTransaction
  - GET /v1/status
external_published_skills:
  note: >-
    https://docs.li.fi/llms.txt states LI.FI capabilities are also published as
    pre-built agent skills on third-party marketplaces. These are not first-party
    LI.FI repositories and were not captured.
  listed_on:
  - https://clawdhub.com
  - https://skills.sh
  - https://playbooks.com
